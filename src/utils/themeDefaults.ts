// 主题兜底色集中定义：避免结果页、分享海报等组件各自散落硬编码的默认强调色

/** 结果主题默认强调色（暖金色）：角色 accent 与原型 accent 均缺失时的兜底色 */
export const DEFAULT_ACCENT = '#e2ad3b'

/** AI 解读卡片默认强调色（品牌绿）：未传入 accent 时的兜底色 */
export const DEFAULT_AI_ACCENT = '#33a474'
